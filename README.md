# kup-ordered-trait-assignment
## assignment to write the data type of Set whose type is upper bounded by ordered trait.
include these two methods
-> **def include(x: A): Set[A]**
-> **def contains(x: A): Boolean**
